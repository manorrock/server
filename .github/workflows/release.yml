name: release
on:
  push:
    tags:        
      - v*
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout sources
      uses: actions/checkout@v1
    - name: Login to Docker Hub
      uses: azure/docker-login@v1
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    - name: Build amd64 image
      run: |
        TAG=${GITHUB_REF}
        INDEX=${TAG%v*}
        VERSION=${TAG:${#INDEX}}
        docker build -f src/main/docker/Dockerfile.amd64 -t manorrock/server:amd64-$VERSION .
        docker push manorrock/server:amd64-$VERSION
    - name: Build arm image
      run: |
        TAG=${GITHUB_REF}
        INDEX=${TAG%v*}
        VERSION=${TAG:${#INDEX}}
        docker build -f src/main/docker/Dockerfile.arm -t manorrock/server:arm-$VERSION .
        docker push manorrock/server:arm-$VERSION
    - name: Push Docker manifest
      run: |
        TAG=${GITHUB_REF}
        INDEX=${TAG%v*}
        VERSION=${TAG:${#INDEX}}
        export DOCKER_CLI_EXPERIMENTAL=enabled
        docker manifest create manorrock/server:$VERSION \
          manorrock/server:amd64-$VERSION \
          manorrock/server:arm-$VERSION
        docker manifest annotate manorrock/server:$VERSION \
          manorrock/server:amd64-$VERSION --os linux --arch amd64
        docker manifest annotate manorrock/server:$VERSION \
          manorrock/server:arm-$VERSION --os linux --arch arm
        docker manifest push manorrock/server:$VERSION
